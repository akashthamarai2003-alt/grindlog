name: Auto Push Notifications

on:
  schedule:
    - cron: '*/15 * * * *'

  # Allow running manually from the Actions tab
  workflow_dispatch:
    inputs:
      type:
        description: 'Type of reminder to trigger'
        required: true
        default: 'dynamic'
        type: choice
        options:
          - dynamic
          - morning
          - afternoon
          - night
          - ai
          - streak
          - tree

jobs:
  trigger-reminders:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Manual Dispatch
        if: github.event_name == 'workflow_dispatch'
        run: curl -X GET "https://www.grindlog.in/api/cron/reminders?type=${{ github.event.inputs.type }}"
        
      - name: Trigger Dynamic Reminders
        if: github.event_name == 'schedule'
        run: curl -X GET "https://www.grindlog.in/api/cron/reminders?type=dynamic"
        
      - name: Trigger Morning Reminder
        if: github.event.schedule == '30 2 * * *'
        run: curl -X GET "https://www.grindlog.in/api/cron/reminders?type=morning"
        
      - name: Trigger Tree Reminder
        if: github.event.schedule == '0 6 * * *'
        run: curl -X GET "https://www.grindlog.in/api/cron/reminders?type=tree"
        
      - name: Trigger Afternoon Reminder
        if: github.event.schedule == '30 7 * * *'
        run: curl -X GET "https://www.grindlog.in/api/cron/reminders?type=afternoon"
        
      - name: Trigger AI Coach Reminder
        if: github.event.schedule == '30 10 * * *'
        run: curl -X GET "https://www.grindlog.in/api/cron/reminders?type=ai"
        
      - name: Trigger Streak Reminder
        if: github.event.schedule == '30 12 * * *'
        run: curl -X GET "https://www.grindlog.in/api/cron/reminders?type=streak"
        
      - name: Trigger Night Reminder
        if: github.event.schedule == '30 15 * * *'
        run: curl -X GET "https://www.grindlog.in/api/cron/reminders?type=night"
